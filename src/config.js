export const config = {
    particleCount: 20000,
    baseColor: '#00ffff',
    template: 'saturn',

    // Interaction state
    rightHandOpenness: 1.0,
    leftHandX: 0.5,
    leftHandY: 0.5,

    // Smoothed transform state
    currentScale: 1,
    currentRotX: 0,
    currentRotY: 0
};
